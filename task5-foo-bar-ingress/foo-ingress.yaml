---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: foo-http-echo
  namespace: foobar
spec:
  rules:
    - host: foo.localhost
      http:
        paths:
          - backend:
              service:
                name: foo-http-echo
                port:
                  number: 8080
            path: /
            pathType: Prefix
